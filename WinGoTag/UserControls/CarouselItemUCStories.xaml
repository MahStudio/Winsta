<UserControl
    x:Class="WinGoTag.UserControls.CarouselItemUCStories"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinGoTag.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Image x:Name="CarouImage" Source="{Binding URI}" MaxWidth="{Binding Width}" MaxHeight="{Binding Height}"/>
        <MediaElement AutoPlay="True" x:Name="CarouVideo" AreTransportControlsEnabled="True" PosterSource="{Binding URI}" Source="{Binding Url}" Tapped="Media_Tapped" MaxWidth="{Binding Width}" MaxHeight="{Binding Height}"/>
    </Grid>
</UserControl>
